{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Initialize Node.js Project",
			"type": "shell",
			"command": "npm init -y && npm install express mongoose dotenv cors bcryptjs jsonwebtoken"
		}
	]
}